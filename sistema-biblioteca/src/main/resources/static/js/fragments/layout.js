/* ============================================================
   LAYOUT.JS — Logiche globali di layout
   ============================================================ */
document.addEventListener('DOMContentLoaded', () => {
    // Logiche generali se necessarie
});
