/**
 * settings.js
 * Logic specifically for the settings page.
 * Most popup actions are handled by popup.js.
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log('Settings section loaded');
});
